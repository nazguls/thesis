import {
	
} from '../actions/types';

const INITIAL_STATE = {
	name: 'Isaac Yoon',
	mktValue: 0,
	cash: 0
};

export default (state = INITIAL_STATE, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
